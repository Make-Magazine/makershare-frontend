Object.defineProperty(exports, "__esModule", { value: true });
var permutations_1 = require("../../variants/permutations");
var transform_1 = require("../../route/transform");
exports.forkRender = function (operation) {
    var operations = new Array();
    for (var _i = 0, _a = operation.routes; _i < _a.length; _i++) {
        var route = _a[_i];
        if (operation.variants == null || Object.keys(operation.variants).length === 0) {
            operations.push({
                scope: operation,
                uri: transform_1.routeToUri(route),
            });
            continue;
        }
        var variants = permutations_1.permutations(operation.variants);
        for (var _b = 0, _c = Array.from(variants.entries()); _b < _c.length; _b++) {
            var _d = _c[_b], variant = _d[0], transition = _d[1];
            operations.push({
                scope: operation,
                uri: transform_1.routeToUri(route),
                variant: variant,
                transition: transition,
            });
        }
    }
    return operations;
};
//# sourceMappingURL=fork.js.map