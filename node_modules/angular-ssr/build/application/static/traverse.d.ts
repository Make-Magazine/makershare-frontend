import { Node, SyntaxKind } from 'typescript';
export declare type NodeVisitor<T extends Node> = (node: T) => boolean;
export declare const traverse: <T extends Node>(root: Node, kind: SyntaxKind, visitor: NodeVisitor<T>) => boolean;
