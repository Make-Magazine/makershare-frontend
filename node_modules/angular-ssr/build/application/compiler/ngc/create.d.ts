import { AotCompiler, StaticReflector } from '@angular/compiler';
import { CompilerHost as NgCompilerHost } from '@angular/compiler-cli';
import { AngularCompilerOptions } from '@angular/tsc-wrapped';
import { CompilerHost, Program } from 'typescript';
import { PathReference } from '../../../filesystem';
export interface CompilerInstance {
    compiler: AotCompiler;
    reflector: StaticReflector;
    host: NgCompilerHost;
}
export declare const createNgCompiler: (compilerHost: CompilerHost, program: Program, options: AngularCompilerOptions, roots: PathReference[]) => CompilerInstance;
