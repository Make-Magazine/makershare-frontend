import { AngularCompilerOptions } from '@angular/tsc-wrapped';
import { CompilerOptions, Program } from 'typescript';
import { ModuleDeclaration, Project } from '../project';
import { PathReference } from '../../filesystem';
export interface CompilationOptions {
    ts: CompilerOptions;
    ng: AngularCompilerOptions;
    sources: Array<string>;
}
export declare const projectToOptions: (project: Project) => CompilationOptions;
export declare const loadApplicationModule: (program: Program, basePath: PathReference, module: ModuleDeclaration) => ModuleDeclaration;
