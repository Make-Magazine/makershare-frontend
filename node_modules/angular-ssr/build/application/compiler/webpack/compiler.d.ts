import { Provider } from '@angular/core';
import { ApplicationCompiler } from '../compiler';
import { ConfigurationLoader } from './config';
import { ModuleLoader } from '../loader';
import { Project } from '../../project';
import { ServerPlatform } from '../../../platform';
export declare class WebpackCompiler implements ApplicationCompiler {
    private project;
    private loader;
    constructor(project: Project, loader: ConfigurationLoader);
    createPlatform(providers: Array<Provider>): ServerPlatform;
    compile(): Promise<ModuleLoader>;
}
