import { NgModuleFactory } from '@angular/core';
import { ModuleDeclaration, Project } from '../../project';
import { ModuleLoader } from '../loader';
export declare type Chunk = {
    name: string;
    files: Array<string>;
};
export declare class WebpackModuleLoader implements ModuleLoader {
    private project;
    private chunks;
    constructor(project: Project, chunks: Array<Chunk>);
    load<M>(): Promise<NgModuleFactory<M>>;
    lazy<T>(module: ModuleDeclaration): Promise<T>;
    dispose(): void;
}
