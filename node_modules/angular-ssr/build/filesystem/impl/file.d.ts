import { FilesystemBaseImpl } from './base';
import { PathReference, FileReference } from '../contracts';
export declare class FileImpl extends FilesystemBaseImpl implements FileReference {
    private owner;
    constructor(owner: PathReference, sourcePath: string);
    exists(): boolean;
    parent(): PathReference;
    create(content: string): void;
    private cachedContent;
    content(): string;
    assert(): void;
    unlink(): void;
    toString(): string;
}
