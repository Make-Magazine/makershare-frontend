import { Location } from '@angular/common';
import { Router } from '@angular/router';
import { Route } from './route';
import { RouteExtractionOperation } from '../application/operation';
export declare const applicationRoutes: <M>(operation: RouteExtractionOperation<M>) => Promise<Route[]>;
export declare const extractRoutesFromRouter: (router: Router, location: Location) => Route[];
export declare const renderableRoutes: (routes: Route[]) => Route[];
export declare const uniqueRoutes: (routes: Route[]) => Route[];
