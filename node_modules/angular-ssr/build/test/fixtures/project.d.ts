import { Project } from '../../application';
import { PathReference } from '../../filesystem';
export declare const getApplicationRoot: () => PathReference;
export declare const getApplicationProject: (moduleId: string, moduleSymbol: string, workingPath?: PathReference) => Project;
