import { Type } from '@angular/core';
import { Observable } from 'rxjs';
import { Application, ApplicationBuilder } from '../../application';
import { Snapshot } from '../../snapshot';
export declare const loadApplicationFixtureFromModule: <M>(moduleType: Type<M>, fn?: (builder: ApplicationBuilder<any>) => void) => Application<{}>;
export declare const renderModuleFixture: <M>(moduleType: Type<M>) => Observable<Snapshot<void>>;
