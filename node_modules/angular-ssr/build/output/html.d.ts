import { OutputOptions } from './options';
import { OutputProducer } from './producer';
import { Snapshot } from '../snapshot';
export declare class HtmlOutput implements OutputProducer {
    private options;
    constructor(options: OutputOptions);
    initialize(): Promise<void>;
    write<V>(snapshot: Snapshot<V>): Promise<void>;
    exception(exception: Error): Promise<any>;
    private routedPathFromSnapshot<V>(snapshot);
}
