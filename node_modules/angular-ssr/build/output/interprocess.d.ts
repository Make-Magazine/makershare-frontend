import { OutputOptions } from './options';
import { OutputProducer } from './producer';
import { Snapshot } from '../snapshot';
export declare class InterprocessOutput implements OutputProducer {
    constructor(options: OutputOptions);
    initialize(): void;
    write<V>(snapshot: Snapshot<V>): Promise<void>;
    exception(exception: Error & {
        originalStack?: string;
        zoneAwareStack?: string;
    }): Promise<void>;
    private send<T>(message);
}
