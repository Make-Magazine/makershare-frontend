export declare class MutationObserver implements MutationObserver {
    constructor(callback: MutationCallback);
    observe(target: Node, init: MutationObserverInit): void;
    disconnect(): void;
    takeRecords(): Array<MutationRecord>;
}
export declare const bindMutation: (target: () => Window) => (boolean | {
    MutationObserver: typeof MutationObserver;
})[];
