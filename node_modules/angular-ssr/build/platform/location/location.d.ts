import { OnDestroy } from '@angular/core';
import { LocationChangeListener, PlatformLocation } from '@angular/common';
import { DocumentContainer } from '../document';
export declare class LocationImpl implements Location, PlatformLocation, OnDestroy {
    private requestUri;
    private documentContainer;
    initializationPromise: Promise<void>;
    private readonly destruction;
    private initialized;
    constructor(requestUri: string, documentContainer: DocumentContainer);
    initializationComplete(): void;
    assign(uri: string): void;
    replace(uri: string): void;
    reload(): void;
    readonly href: string;
    readonly host: string;
    readonly hostname: string;
    readonly origin: string;
    readonly port: string;
    readonly protocol: string;
    getBaseHrefFromDOM(): string;
    onPopState(fn: LocationChangeListener): void;
    onHashChange(fn: LocationChangeListener): void;
    readonly pathname: string;
    readonly search: string;
    readonly hash: string;
    replaceState(state: any, title: string, url: string): void;
    pushState(state: any, title: string, url: string): void;
    forward(): void;
    back(): void;
    ngOnDestroy(): void;
    private parsed<T>(fn);
}
