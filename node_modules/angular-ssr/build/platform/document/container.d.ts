import { OnDestroy } from '@angular/core';
export declare class DocumentContainer implements OnDestroy {
    private windowRef;
    constructor(templateDocument: string, requestUri: string);
    readonly window: Window;
    readonly document: Document;
    complete(): void;
    ngOnDestroy(): void;
    private cloneFrom(document);
}
