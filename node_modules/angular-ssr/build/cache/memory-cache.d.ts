import { Application } from '../application';
import { Cache } from './cache';
import { Snapshot } from '../snapshot';
export declare class MemoryCache implements Cache {
    private application;
    private cacheSize;
    private ttl;
    private cache;
    constructor(application: Application<void | {}>, cacheSize?: number, ttl?: number);
    get(uri: string): Promise<Snapshot<void>>;
    has(uri: string): boolean;
}
