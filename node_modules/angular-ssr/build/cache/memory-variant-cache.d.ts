import { Application } from '../application';
import { Cache } from './cache';
import { Snapshot } from '../snapshot';
export declare class MemoryVariantCache<V> implements Cache {
    private application;
    private cacheSize;
    private ttl;
    private cache;
    constructor(application: Application<V>, cacheSize?: number, ttl?: number);
    get(uri: string, variants: V): Promise<Snapshot<V>>;
    has(uri: string, variants: V): boolean;
}
