import { Scope } from '../scope';
import { Stream } from '../stream';
export declare class Logger implements Scope {
    parent: Logger;
    description: string;
    additionalStreams: Array<Stream>;
    constructor(parent: Logger, description: string, additionalStreams?: Array<Stream>);
    newScope(description: string, additionalStreams?: Array<Stream>): Scope;
    info(message: string, ...args: Array<any>): void;
    debug(message: string, ...args: Array<any>): void;
    warn(message: string, ...args: Array<any>): void;
    error(message: string, ...args: Array<any>): void;
    dispose(): void;
    private traverseUpward(level, scope, message, args);
}
