import { Level } from '../../level';
import { Scope } from '../../scope';
import { Stream } from '../stream';
import { RollingOptions } from './options';
export declare class RollerStream implements Stream {
    private options;
    private fileDescriptor;
    private rollTimer;
    constructor(options?: RollingOptions);
    buffer(level: Level, scope: Scope, message: string, args: Array<any>): void;
    dispose(): void;
    private opened();
    private readonly currentPath;
    private open();
    private roll();
    private resolvePrevious(index);
    private archiveCurrent(previous);
    private shiftPrevious(files);
    private collectPrevious();
}
