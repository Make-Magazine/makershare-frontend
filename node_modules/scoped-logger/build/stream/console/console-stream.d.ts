import { Stream } from '../stream';
import { Scope } from '../../scope';
import { Level } from '../../level';
export declare class ConsoleStream implements Stream {
    buffer(level: Level, scope: Scope, message: string, args: Array<any>): void;
    dispose(): void;
}
