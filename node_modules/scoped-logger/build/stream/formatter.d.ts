import { Level } from '../level';
import { Scope } from '../scope';
export declare abstract class Formatter {
    static conditionalColorized(level: Level, scope: Scope, message: string, args: Array<any>): string;
    static simpleText(level: Level, scope: Scope, message: string, args: Array<any>): string;
    static colorizedText(level: Level, scope: Scope, message: string, args: Array<any>): string;
}
